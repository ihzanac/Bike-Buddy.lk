import { BikeSaleShopView } from '@/components/bikes/BikeSaleShopView'

/** Full bike sale shop experience (static HTML parity) inside customer shell */
export function CustomerBikesPage() {
  return <BikeSaleShopView layout="customer" />
}
